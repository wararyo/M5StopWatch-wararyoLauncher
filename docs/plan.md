# wararyoLauncher 設計書

更新日: 2026-09-20。旧設計メモを本書へ統合する。

本書は製品版の実装方針であり、実装済み機能の一覧ではない。測定用試作は `measurement/`、測定事実と再現手順は [measurements.md](measurements.md) を参照する。合意した要件と、実機調整できる既定値を区別して記述する。

## 1. 目的と初版の範囲

M5StopWatch-MultiFirm の `ota_0` で動作する、軽量・省電力なホストを作る。時計をホームにし、全画面を素のM5GFXで描画する。LVGLは使用しない。Wi-Fi・BLEを初期化せず、無線経由の設定・更新は実装しない。

| 機能 | 初版 |
|---|---|
| ホーム | デジタル時計1種類。WatchFace追加が可能な構造 |
| アプリ一覧 | 時計から上スワイプで表示。物理ボタンでも操作可能 |
| ストップウォッチ | 開始・停止・再開・リセット・ラップ |
| 設定 | 日時の手動設定、輝度、消灯時間、バージョン等の情報表示 |
| 外部アプリ | 1〜3を表示し、それぞれ `ota_1`〜`ota_3` を起動。アイコンは数字の埋め込み画像 |
| 共通入力 | Aが次へ、Bが決定、A+Bを連続600ms保持でホーム |
| 時刻 | 表示・手動入力はJST（UTC+9）固定 |
| 電源 | 無操作で画面OFF、入力で復帰。CPUはまず240MHz固定 |
| 技術スタック | PlatformIO + ESP-IDF + M5Unified。描画はM5GFX |

アラーム、タイマー、バッジ、追加WatchFaceは将来機能。未実装アプリのダミー項目は初版に並べない。バッジの画像転送は将来PCから有線で行う。

### 1.1 内蔵画面と外部アプリの境界

| 状況 | 初版 | 将来 |
|---|---|---|
| 内蔵画面間の移動・ホーム・画面OFF | ストップウォッチ計測を継続 | 同左 |
| 外部アプリへ切り替え | ストップウォッチ停止。再起動をまたぐ値の保存・復元なし | MultiFirm APIで経過時間を復元 |
| 外部アプリ中のタイマー・アラーム | 通知しない。初版では機能自体を実装しない | MultiFirm APIとゲストの協力で通知 |
| RTC・タイムゾーン | ホスト内の規約だけを定義。ゲストとの統一は保証しない | MultiFirm共通APIに移行 |
| 外部アプリからの復帰 | 必ず時計画面 | 同左 |
| USB時刻設定 | 実装しない | MultiFirm側のツール/APIを検討 |

外部アプリ起動中にホストは動作しない。共有状態をランチャー独自のNVS形式で先行実装しない。

## 2. 測定結果と採用判断

### 2.1 描画方式

試作の描画対象はM5GFXが提供する468×468のRGB565フレームバッファ。設計画像や公称の466角を固定せず、実装では `M5.Display.width()/height()` から領域を取得する。円形画面端には文字・操作対象の安全領域を設ける。

| シナリオ | 平均描画時間 | 最悪描画時間 |
|---|---:|---:|
| 一覧スクロール | 13.6ms | 25.6ms |
| 時計→一覧 | 12.1ms | 17.3ms |
| 時計の分更新 | 3.8ms | 13.8ms |
| 時計静止 | 描画0回/120回 | — |

採用方針:

1. M5GFX所有のPSRAMフレームバッファへ直接描画する。中間の全画面スプライトは追加しない。
2. 前フレームを保持し、変化した要素と影響を受ける要素だけ再描画する。
3. 画面外要素をカリングし、大きな時刻文字など効果を測定済みの部品をキャッシュする。
4. データキャッシュ64KiB／ライン64Bを基準にし、内部RAMを32KiB多く使う分を予算へ含める。

上記は描画処理時間であり、入力・待機を含む製品版の実fps保証ではない。外部アプリ2・3、日本語、円形に沿う一覧、設定画面等を追加した状態で再測定する。

### 2.2 電力の評価

CPU 240MHz固定、light sleepなし、満充電、充電中でないUSB給電条件での代表値。目視読みの確度は約±2mA。

| 条件 | 結果 | 判断 |
|---|---:|---|
| 時計静止・輝度90 | 38mA | アイドル約40mA目標に沿う基準値。余裕は小さい |
| 画面OFF | 38mA | 当初のUSB側20mA目標は未達。PM導入後に再測定 |
| 最大速度の一覧スクロール | 55mA | 当該負荷条件の値で、装置全体の最大電流ではない |
| 全白30fps | 62mA | 表示内容による増加を確認 |

時計表示と消灯の差は今回の精度では検出できなかった。「画面の消費がゼロ」とは扱わない。消灯は不要な点灯と固定表示の継続を減らすためにも実装する。

描画デューティと電流の線形関係は確認されていない。デューティは描画負荷の指標であり、電流の代用にはしない。

当初の20mA／40mAはUSBテスター側の目標として保持する。電池側のmAは電圧・変換効率・USB動作条件が異なるため、USBの38mAを電池側電流の上限とみなしたり、直接差し引いたりしない。電池駆動の消費電力・稼働時間は別に評価し、目標稼働時間は基準測定後に決める。

測定記録の数値は変更せず保持する。ただし同記録の「表示していても電力を消費していない」「最悪55mA」「38mAは上限値」という解釈は、製品仕様では本節の限定付き評価を採用する。

## 3. ビルドとハードウェア

試作とMuteHidに合わせ、以下を初期基準として版を固定する。

| 項目 | 基準 |
|---|---|
| PlatformIO | `espressif32 @ 6.12.0`、`framework = espidf` |
| IDF | 測定記録は5.5.0。実際に解決された版をビルドログに記録 |
| board設定 | 試作の `m5stack-cores3` を出発点にStopWatch認識・構成を確認 |
| ライブラリ | M5Unified 0.2.16、M5GFX 0.2.28 |
| CPU | 初版240MHz固定。DFS・light sleepは後続段階 |
| メモリ | 16MiB Flash、DIO 80MHz、Octal PSRAM 80MHz |
| キャッシュ | 命令16KiB、データ64KiB、データライン64B |
| RTOS | 試作のCPU1配置・1kHz tickを基準に待機とWDTを検証 |
| MultiFirm | 検証済みコミット/タグに固定、`MULTIFIRM_HOST=1` |

命令キャッシュ32KiBとFlash QIOは試作構成で起動失敗があるため採用しない。全SDK・全ブートローダ共通のハードウェア制約とは断定しない。依存更新時にはMultiFirmの既存ブートローダとの互換性も再確認する。

表示・タッチ・ボタン・RTC・必要な電源管理を初期化する。IMU・マイク・スピーカー・Grove 5V出力は初版では無効にする。

## 4. 責務と所有権

| モジュール | 責務 | 境界 |
|---|---|---|
| HostRuntime | 初期化、イベント配送、期限管理、サービス接続 | 業務状態と描画実装を持たない |
| ScreenManager / LauncherController | 画面の入退場とホーム処理 / ホーム・一覧の操作と遷移 | HAL・NVS・OTAを直接呼ばない |
| LaunchRegistry | 安定ID、表示名、アイコン、内蔵/外部の起動先 | 外部スロットを配列位置から推測しない |
| 内蔵アプリ画面 | 操作要求と表示モデル | 計測状態を画面に持たない |
| InputController | タッチ・A/B・同時長押しを意味イベントに変換 | 画面遷移や描画はしない |
| Renderer / FramePlan | レイヤー全体の計画、消去、描画、転送 | アプリ状態・時刻・電源を変更しない |
| WatchFace | 与えられたデータの描画と次回更新期限 | 入力・RTC・NVS・電源にアクセスしない |
| TimeService | RTC検証、システム時刻同期、JST変換、手動設定 | 時刻変更イベントを発行 |
| StopwatchService | 計測・停止・再開・リセット | 壁時計と描画周期に依存しない |
| PowerManager | 消灯期限、復帰、電源ポリシー、将来のPMロック | 画面内容を決めない |
| SettingsStore | ローカル設定の検証・保存・版管理 | 共有NVSを全消去しない |
| MultiFirmAdapter | スロット列挙、起動要求、結果通知 | 検証・OTA処理を複製しない |
| HAL | M5Unified/ESP-IDF経由の周辺機器操作 | 値と成否を上位へ返す |

UI状態とM5GFXは単一UIタスクが所有する。重いスロット検証は専用ワーカーで直列実行し、結果をイベントでUIへ返す。ワーカーは画面やUI状態へ直接触れない。他サービスは原則UIタスクから呼び、不要な常駐タスクを増やさない。共有I2C操作も直列化する。

製品版はルートの `src/` 以下を `host/`, `input/`, `ui/`, `features/`, `services/`, `power/`, `storage/`, `multifirm/`, `hal/` に分ける。`measurement/` は基準測定用として保持する。

### 4.1 WatchFace契約

試作の `begin / plan / paint / nextUpdateDelayMs` を基礎にする。

- 安定したIDで登録する。初版はデジタル1種類で、文字盤選択UIは作らない。
- 初期化時にキャッシュを確保し、終了時に解放する。フレームごとの動的確保を避ける。
- `plan` はJST時刻、有効性、電池情報、充電状態、表示領域、遷移オフセットを受け、描画要素を登録する。
- 全レイヤーのplan後にRendererが解決・消去し、その後paintする。同一フレームの入力データは不変。
- 次回更新期限をRuntimeが実際に使用する。デジタルは分境界、将来の秒表示は秒境界を指定できる。
- 時刻設定・電池更新・復帰・文字盤切り替えでも無効化する。
- キャッシュ確保失敗時は直接描画に戻し、時刻自体を表示不能にしない。

内蔵アプリ画面は入場・退場、意味イベント処理、表示モデル生成、次回表示更新期限を提供する。ホームは画面へ届く前にScreenManagerが処理する。画面退場は計測停止を意味せず、未保存編集など画面固有の一時状態だけを破棄する。

## 5. 操作と画面

### 5.1 共通の物理入力

A短押しは次の操作対象へ進み、末尾から先頭へ循環する。B短押しは選択中の操作を決定する。短押しはリリース時に確定する。

これは選択と決定で操作する画面の既定であり、全画面へ強制する規約ではない。内蔵アプリ画面はA短押し・B短押しへ画面ごとの意味を割り当ててよく、その場合は画面上のボタン表示でどちらが何をするかを示す。時計・一覧・設定は既定に従う。ストップウォッチは従わない（5.3節）。

画面ごとに変えられるのはA短押しとB短押しの意味だけで、短押しをリリースで確定すること、A+B連続600msがホームであること、ホームを画面固有処理より優先することは例外なく全画面へ適用する。

A+Bは両方が押された時点から600msを数える。一方でも離れたら連続保持時間をリセットする。一度でも同時押しになった押下群は、600ms未満で解除してもA/B短押しへ変換しない。ホーム発火は1回限りで、両方離すまで再発火しない。

タッチ中もホームを優先し、ドラッグ・選択・未保存編集をキャンセルする。時計上のホームは無害な操作とする。対象はホスト内の全画面・ダイアログ・通常の検証待ち。画面OFFなら復帰して時計を表示する。

外部アプリ中の復帰はMultiFirmゲストAPIの責務であり、現行既定は1.6秒。ホストの600ms規約をゲストへ暗黙に適用しない。起動先設定から再起動までの確定処理は取り消せない（8.2節）。

### 5.2 時計・一覧

時計は [WatchFaceDigital.png](WatchFaceDigital.png) を基準に黒背景、電池、日付、時分、APPSを配置する。初期仕様は画像に合わせた英語日付と24時間表示。

上ドラッグに追従して時計が上へ退き、一覧が下から出現する。試作の50px相当を確定しきい値の初期値とし、画面サイズに対する比率で調整可能にする。解放時は短い補間で時計または一覧へ収束する。補間はフレーム数でなく経過時間で進める。時計ではA/B、またはAPPSタップでも一覧を開く。

一覧は [AppList.png](AppList.png) を基準に、ストップウォッチ、設定、外部アプリ1〜3の5項目。円形画面に沿う配置を適用し、試作の固定X座標からの変更を再測定する。

Aで選択を進め、選択項目が見える位置へスクロールする。Bまたは項目タップで開く。ドラッグしきい値を超えたら解放までタップを抑止する。指追従と解放後の位置補正に加え、2026-09-21に解放速度から着地行を決める短距離の慣性スクロールを試験実装した（実機調整は未完了）。一覧先頭での下スワイプは時計へ戻り、それ以外は一覧スクロールを優先する。

空・不正スロットも番号と状態を表示するが起動対象にしない。長い名前は幅に合わせ省略する。省略前の名前は外部アプリ画面で読めるが、Readyのスロットは決定すると直接起動するため、その機会は検証中に限られる（8.2節）。固定の日本語UIに必要なグリフを含め、外部名の未収録文字は代替表示する。任意のUnicode全文字収録は初版の保証外。

一覧の行はアイコンとアプリ名だけで構成し、起動可否の理由文を常設しない。アイコンは直径64px（選択時68px）のアンチエイリアス円と、その上に重ねる44pxのグレースケールマスクで描く。マスクは埋め込み資産で、プリミティブの組み合わせでは描かない。名前はGenShinGothic 28pxのVLWを部分集合化して埋め込み、ASCII・かな・UIが使う約物と固定文言の漢字を収録する。生成手順と退避動作は[作業3の描画基盤](task3/rendering.md)に記す。

### 5.3 ストップウォッチ

状態は `Reset / Running / Paused`。サービスが累積時間と今回の開始時刻を保持し、64bit単調時刻から算出する。日時変更・描画周期から独立させる。

この画面は5.1節の既定に従わず、A短押しとB短押しへ固定の操作を割り当てる。左右2つのボタンを常に表示し、Aが左、Bが右に対応する。同じボタンをタッチでも押せる。カーソル移動はない。

| 状態 | 左＝A短押し | 右＝B短押し |
|---|---|---|
| Reset | ラップ（無効） | 開始 |
| Running | ラップ | 停止 |
| Paused | リセット | 再開 |

一覧へ戻る操作は持たない。A+Bのホームで時計へ戻る。ホーム・画面退場・消灯では計測を継続する。

ラップはRunning中だけ受け付け、通算の番号を振って記録する。表示は新しいものが上で、画面に収まる最新3件だけを見せる。スクロールと永続化は持たない。

表示は `HH:MM:SS.cc`（1/100秒）を既定とし、桁上限では上限表示して内部値は維持する。表示分解能は実機の計測精度保証を意味しない。実行中の可視画面は25ms周期（40Hz）で更新し、6.3節の30fps超に余裕を持たせる。他画面・消灯中は表示更新を止め、再表示時に経過時間を算出する。電源断・再起動・外部切り替え後の値の復元は初版では行わない。

2026-09-22に、UI を M5StopWatch-UserDemo のストップウォッチへ合わせる方針を決めて上記へ改めた。旧仕様はAで選択・Bで実行する既定どおりの画面で、選択肢に「戻る」を持ち、ラップを記録しないものだった。ランチャーの主目的から見て、計測中に最もよく押すのは開始・停止とラップであり、それを1手で押せることを優先した。その結果A/Bが埋まるので「戻る」を落とし、5.1節を画面ごとの裁量を認める書き方へ改めた。

### 5.4 設定

輝度90、消灯30秒を調整可能な既定値とする。輝度設定には可視性を保てる下限を設ける。日時は年・月・日・時・分を編集し、保存時に秒を0へ合わせる。

Aでフィールド/候補を送り、Bで編集/確定する。「保存」「キャンセル」を明示し、ホームでは未保存編集を破棄する。輝度のプレビューもキャンセル時に保存値へ戻す。妥当な日付と範囲を保存前に検証する。

設定には保存する値のほかに、決定した時点でその場で効き、保存せずに再起動まで持続する項目を置ける。
この種の項目は起動時に必ず無効から始まり、実行中に無効へ戻す操作を持たない。
戻せないことは、決定後にその項目自身の表示で示す。

## 6. 描画と実行周期

### 6.1 FramePlanの不変条件

1. 全レイヤーの要素の旧矩形・新矩形・内容変更を収集する。
2. 消去と再描画の影響を受ける他要素を特定する。
3. 必要な消去をすべて終える。
4. 背面から前面へ描画する。
5. M5GFXのダーティ領域を転送する。

円は両端を含む `2r+1`、文字は張り出しを含む矩形を使う。消える要素を単に登録から外して消し残さない。画面切り替え・復帰・重なるモーダル/トーストの表示と消去では履歴を破棄した全面再描画を許可する。

初版の基本要素は黒背景を前提とする。背景画像や常時重なりを持つWatchFaceは、背景復元と前景再描画を追加してから対応する。試作の容量12要素を無条件で流用せず、最大要素数を把握し、超過時は診断と全面再描画への退避を行う。

468×468×2bytesは約428KiB。M5GFXのバッファに同等の中間バッファを重ねない。文字キャッシュ、日本語フォント、ワーカーを含め、内部RAM/PSRAMの空き・最大連続領域・スタック余裕を記録する。ピクセル比較用バッファは通常ビルドに含めない。

### 6.2 更新・待機

描画の契機は入力、遷移の次フレーム、表示データの期限、サービス状態変化、復帰。静止中はRendererを毎ループ呼ばず、レイアウト計算も省略する。時計は分境界、電池表示は30秒間隔、時刻設定・復帰は即時更新。画面OFF中は表示期限のために起こさない。

操作中は16ms程度の目標周期から開始する。期限超過時は古いフレームを追いかけず最新状態へ進み、次期限の基準を取り直して最低1tickブロックし、アイドルタスクへ実行機会を渡す。`vTaskDelayUntil` が期限超過で戻り続ける状態を放置しない。WDT無効化・タイムアウト延長を対策にしない。

入力はボタン・タッチの割り込み通知か次の期限まで待ち、ボタンまたは指が押されている間だけ入力周期（10ms）で追跡する。割り込みが使えないときは入力周期のポーリングへ戻る。ISRでは通知だけを行い、I2C・描画を行わない。バックグラウンドの作業者も結果を出したらUIタスクへ通知する。期限は64bit単調時刻を基本とし、32bitならラップアラウンドを扱う。

### 6.3 性能基準

操作中の継続描画は30fps超を目標とし、描画処理時間と描画完了間隔を別々に測る。規定操作の連続区間で実fpsが30を超え、定常区間のフレーム間隔が33.3ms以内であることを受け入れ基準にする。

平均・p95・最大・33.3ms超過数を記録する。初回描画、復帰、外部起動待ちは別集計。静止中の0fpsは正常で、分更新・電池更新の単発描画を許す。入力→表示は100ms以内、消灯復帰は300ms以内を暫定目標として測定する。暫定値は実機で調整できるが、測らず達成扱いしない。

実fpsと描画処理時間は、通常のファームウェアでも画面上で読めるようにする。この表示はフレーム計画の外に置き、
描画したフレームの最後に自分の矩形だけを塗り直す。更新期限を持たないので、静止中の描画0回を変えない。
表示の有無で測定値が変わらないことを確認したうえで基準測定に用いる。

## 7. 電源・時間管理

### 7.1 電源状態

| 状態 | 用途 | 初版 | PM段階 |
|---|---|---|---|
| Active | 操作・遷移・可視の計測更新 | 240MHz、必要な描画 | 必要な期間だけ性能を確保 |
| WatchIdle | 静止表示。設定等の静止画面も含む | 240MHz、再描画抑制 | DFS。light sleepはしない |
| ScreenOff | パネルsleep、描画なし | 240MHz、待機 | DFS。USB給電がなければ入力・期限で復帰するlight sleep |
| UsbActive | USB通信・診断維持 | 240MHz、通信維持 | 必要なPMロック |

USBと表示状態は同時に成立するため、内部は表示/操作状態と `usbSessionActive` の2軸とする。UsbActiveは電源ポリシーの上書きで、USB接続だけで常時点灯しない。USB切断時は元の表示状態に対応するポリシーへ戻す。測定モードだけが明示的に消灯を上書きできる。

操作・アニメーションが止まればWatchIdle、最終操作から30秒でScreenOff。ボタン保持・タッチ継続は操作と数えるが、時刻更新・電池更新・計測継続では消灯期限を延ばさない。

消灯時はパネルsleepと描画停止。復帰後は全体無効化し最新状態を描画する。通常復帰は直前の画面、外部からの再起動とホーム操作は時計画面。

最初のタッチは解放までジェスチャ全体を復帰専用として消費する。物理ボタンは復帰後も通常処理するが、A+Bの短押し抑止を先に適用する。

### 7.2 省電力化の順序

240MHz固定で機能・性能の基準を確立した後に、イベント待機、I2Cポーリング削減、DFS、tickless idle、自動light sleepを段階導入する。施策ごとに電力・性能・入力・USB・復帰を比較する。

deep sleepと電源OFFは計測保持・RTC復帰の別設計が必要なので初版対象外。VBUSと実際のUSBデータ接続は区別する。light sleepはMuteHid同様にVBUSがある間は禁止し、電池駆動のときだけ許す（2026-09-23決定）。そのためlight sleepの効果は電池駆動でだけ評価し、USB側の電流を下げる手段はDFSとポーリング削減に限られる。
light sleepはパネルがsleepしている間だけ許す。表示の転送はPMのロックを持たないため、パネルが動いている間に眠ると転送を壊しうる。許可はパネルをsleepさせた後に出し、パネルを起こす前に取り消す。VBUSの読み取りに失敗したときは許さない。

### 7.3 時計

ホスト内部は試作に合わせRTCをUTCとして読み書きし、表示と手動入力だけJSTへ変換する。これはゲストとの共有契約ではなく、他ファームウェアのRTC解釈・変更との整合は保証しない。タイムゾーン設定UIと共有保存は作らない。

起動時にRTC取得結果、日付時刻の範囲、取得可能な有効性情報を確認する。無効なら `--:--` と設定への導線を表示する。システム時計へ同期し、毎描画のRTC読み出しを避ける。

手動保存は検証→UTC変換→RTC書き込み→システム時計更新→表示更新とし、失敗を成功表示しない。途中失敗ならRTCを読み直して整合を回復し、回復できなければ無効状態を明示する。PM導入後はsleepをまたぐ時計・計測の連続性を実機確認する。

## 8. MultiFirm・保存・更新

### 8.1 API

2026-09-20時点の隣接リポジトリには `multifirm_host.h/.cpp` が実装されている。旧メモのAPI未実装を前提にせず、以下を利用する。

- `isMultiFirmLayout()`：レイアウト確認。
- `scanSlots()` / `inspectSlot()`：検証状態、名前、バージョン等の取得。
- `bootSlot(index, shutdown, context)`：再検証、起動先設定、終了処理、再起動。

まず時計を表示し、検証はワーカーで行う。一覧には検証中を表示し、Readyのみ起動可能とする。Empty / Invalid / ReadErrorは区別して表示する。名前の優先順位とメタデータ照合はライブラリに任せる。番号アイコンとスロットは固定対応。

非対応レイアウトでは外部起動を無効にし、内蔵アプリは利用可能にする。検証・起動・復帰時間を診断する。

### 8.2 起動確定とホーム

現行 `bootSlot` は同期・キャンセル非対応で、起動先設定後にvoid型shutdownを呼んで再起動する。単にワーカーへ移すだけではホーム後の遅延再起動を防げないため、次の境界を設ける。

1. 一覧でReadyのスロットを決定した時点を最終決定とする。確認画面は挟まない。計測中でも確認しない。
2. 最終決定前に失敗し得る設定保存等を完了する。失敗なら起動しない。
3. 最終決定後は `BootCommitting` とし、起動中表示を出してAPIを1回だけ呼ぶ。この区間の取り消しは保証せず、ホームを含む新しい操作を抑止する。
4. API失敗ならエラー表示へ戻し、ホームを再び受け付ける。ストップウォッチは継続する。
5. 成功時shutdownで同期を確保して計測・音・振動等を止める。失敗し得る必須保存をここへ置かない。

2026-09-22に1を変更した。旧仕様は詳細画面に「起動」「キャンセル」を出し、計測中は「計測を終了して起動」と示すものだった。ランチャーの主目的が起動であり、毎回の確認が操作を1手増やすだけだったため直接起動に改めた。計測中の確認も行わないので、外部アプリ起動は計測を黙って終了させる（1.1節）。

外部アプリ画面はReady以外のスロットの状態表示、検証中の表示、起動失敗の表示に使う。Readyのスロットの名前・バージョンは、検証中に開いたまま走査が終わった場合に読める。走査完了を起動の契機にはしない。

通常のスロット走査中のホームは即時に時計へ戻り、後から届く結果はスロット情報だけ更新する。起動自体のキャンセルが必要になったら、MultiFirm側に検証と確定を分けるAPIを検討する。

### 8.3 永続化・書き込み保護

既定 `nvs` 内の専用名前空間 `launcher` に、スキーマ版付きの設定レコードを保存する。複数ホストの共存は想定せず、この名前をホスト用として使用する。輝度・消灯時間は確定時だけ書き、描画・毎秒計測ではFlashへ書かない。

未知の版、不正値、NVS初期化失敗は診断し、必要ならRAM上の既定値で動作する。NVS全体を自動消去しない。専用名前空間でも他ファームウェアの全消去からは保護されないため、MultiFirm側の共有NVS運用にも従う。

ホスト上限は4MiBで、ビルド後とインストール時に確認する。製品版のパーティション表はMultiFirm正規レイアウトに一致させ、試作単体用CSVを流用しない。

更新は `install-host` を使用し、通常のupload/erase/uploadfsによる全体上書きを既定で禁止する。初回導入・ブートローダ変更・復旧はMultiFirmの手順に従う。ホスト破損をrecoverだけで直せるとは扱わない。

`multifirm_meta` は読み取り専用、`multifirm_nvs` は将来API用で初版では使わない。バッジ導入時はstorageの形式と所有ディレクトリを調整し、マウント失敗時の自動フォーマットを禁止する。

## 9. 検証と受け入れ

| 分野 | 必須確認 |
|---|---|
| 入力 | A/B、時間差の同時押し、599/600ms境界、片方解除、再保持、タッチ併用、ホーム1回発火 |
| ホーム | 全内蔵画面、設定編集中、ダイアログ、走査中から時計へ。未保存編集を破棄、計測は継続 |
| 計測 | 開始・停止・再開・リセット・ラップ、他画面、消灯、日時変更、長時間動作 |
| 時計 | 無効RTC、取得失敗、うるう日・月末・JST日付境界、保存失敗、復帰更新 |
| 描画 | 全面再描画とのピクセル一致。既存24パターンに往復・端位置・5項目・日本語・トースト・復帰・文字盤差し替えを追加。差分計画の外で描く表示は検証時に無効化し、一致検証の対象外とする |
| 性能 | 製品画面で6.3節の指標。スクロール・遷移各60秒以上、WDT・描画破綻なし |
| 過負荷 | 意図的な周期超過でもアイドルタスクが走り、追いつきループ・WDT・操作不能なし |
| メモリ | キャッシュ確保失敗時の表示継続、反復遷移でリークなし、要素容量超過の検出 |
| 電源 | 無操作消灯、保持中の非消灯、タッチ消費、ボタン復帰、USB抜き差し、PM後の取りこぼしなし |
| MultiFirm | 各スロット正常・空・破損・読取失敗、非対応表、起動失敗、ゲストから時計へ復帰 |
| データ保護 | ホスト更新でゲスト・設定維持、NVS異常で全消去しない、通常upload保護 |

再描画のピクセル検証は描画変更時に必須。検証機能は通常ビルドでは無効にする。入力・時間・状態遷移はHALから分離してホスト側テストを可能にする。

電力は充電状態、VBUS、表示、輝度、CPU/PM設定、ファームウェア版、測定器精度を記録する。USBと電池は別表とし、電池は点灯時間・操作頻度を固定して稼働時間を比較する。

PMIC低レベル診断は測定済みのI2C 100kHzと成功/失敗を返すAPIを使い、失敗値0を正常値と誤認しない。電圧・残量推定から電流を断定せず、消費電流は外部計測する。

## 10. 残る判断

初版開始を妨げる未決定事項はない。輝度90、消灯30秒、スクロールしきい値、遅延の暫定目標は実機調整可能な設計値とする。スコープ・基本操作を変える場合は再合意する。

MultiFirm共有API、外部中の通知、経過時間復元、USB時刻設定、バッジ転送、電池駆動の目標時間は各導入段階で決める。

## 11. 実装計画

以下を個別に実装・確認できる作業単位とする。チェックは実装と完了条件の確認を終えてから付ける。試作で確認済みの項目も、製品版への移植後に必要な検証を行う。

作業1〜7を240MHz固定の初版、作業8をその後の省電力改善とする。20mA目標の達成は作業8で評価し、初版の機能完成と混同しない。

2026-09-22に作業6を作業5より先に実装した。作業6の実装本体は作業5へ依存せず、依存は完了条件の2項目
（API失敗時の計測継続、成功時の計測終了）に限られるため。詳細は[作業6の計画](task6/plan.md)。

| 作業 | 内容 | 依存 |
|---|---|---|
| 1 | 製品用ビルド・書き込み保護 | なし |
| 2 | Runtime・共通入力・画面遷移の基盤 | 1 |
| 3 | 描画基盤・WatchFace・5項目一覧 | 2 |
| 4 | 時計・設定保存・設定画面 | 3 |
| 5 | ストップウォッチ | 4 |
| 6 | MultiFirmホスト連携 | 4（本体）／5（計測連携の2項目のみ） |
| 7 | 初版の統合試験と基準測定 | 6 |
| 8 | 段階的な省電力化 | 7 |

### 作業1: 製品用ビルドと書き込み保護

2026-09-20完了。PC検証とユーザーによる実機確認の結果は
[作業1の検証記録](task1/task1-validation.md)、導入手順は
[製品用ビルド・導入・復旧](task1/product-build.md) を参照。

- [x] ルートにPlatformIO / ESP-IDFの製品用構成と最小の起動処理を作る。3章の依存版・CPU・メモリ設定を固定する。
- [x] MultiFirm正規レイアウト、ホスト定義、4MiB上限検査を組み込む。
- [x] 通常upload・erase・uploadfsの保護と、`install-host` による更新手順を用意する。
- [x] 起動ログへファームウェア版、IDF版、ボード認識、表示サイズ、CPU、メモリ構成を出す。

成果物は製品用ビルド設定、最小ファームウェア、導入・復旧手順。試作の単体書き込み手順と明確に分ける。

完了条件: クリーンビルドが成功し、容量検査と危険な標準書き込みの拒否を確認できる。バックアップ済みの試験機でMultiFirmのホストとして起動し、ゲスト領域を上書きしない更新経路を確認する。

### 作業2: Runtime・入力・画面遷移の基盤

2026-09-20完了。過負荷時の入力停止を修正し、PC検証とユーザーによる実機再確認を完了した。
構成・再現手順は[作業2の基盤と実機手順](task2/runtime.md)、
初回不具合・修正・再確認の結果は[作業2の検証記録](task2/task2-validation.md)を参照。

- [x] HostRuntime、ScreenManager、LaunchRegistry、HAL境界を作り、UI状態を単一タスクが所有する構造にする。
- [x] A/B短押し、A+B連続600ms、タッチの意味イベントを実装する。ホームを画面固有処理より優先する。
- [x] 単調時刻による期限管理と、期限超過時に基準を取り直してブロックする待機処理を実装する。
- [x] PowerManagerに表示状態とUSB状態を分けて持たせ、30秒消灯・復帰・復帰タッチの消費を実装する。CPUは240MHz固定のままとする。

成果物は表示内容に依存しない実行・入力・電源の基盤。この段階では最小の確認画面を使用してよい。

完了条件: 短押し混入、連続保持の解除・再保持、二重ホームを状態遷移テストで確認する。実機で消灯・タッチ/ボタン復帰・USB抜き差しを確認し、意図的な処理超過でもアイドルタスクが動作してWDTが発火しない。

### 作業3: 描画基盤・時計・アプリ一覧

実装順序・変更箇所・検証条件は[作業3の詳細計画](task3/plan.md)を参照。
2026-09-20完了（ユーザーによる実機確認済み）。完了後に一覧アイコンを埋め込み画像へ変更したため、
その分の実機確認は再度必要。構成・実機手順は[作業3の描画基盤](task3/rendering.md)、確認状況は
[作業3の検証記録](task3/task3-validation.md)を参照。性能値は取得済みだが、30fps超・定常間隔33.3ms以内の達成判定は
操作停止時間を分けた作業7の再測定へ引き継ぐ。

- [x] 試作の直接描画、Element / FramePlan、時刻文字キャッシュを製品構成へ移植する。容量超過とキャッシュ確保失敗を扱う。
- [x] WatchFaceの登録・ライフサイクル・更新期限を接続し、デジタル時計を実装する。
- [x] 5項目の一覧、日本語表示、円形画面に沿う配置、ドラッグ追従、位置補正、物理ボタン選択を実装する。
- [x] 重なる表示、画面切り替え、消灯復帰での無効化を実装し、ピクセル比較検証を移植・拡張する。

成果物は時計と一覧を操作できるUI。時計データは次作業まで検証用の値を注入できるようにし、未実装の起動先は起動可能と表示しない。

完了条件: 全面再描画との比較が一致する。入力・期限がない静止時には描画もレイアウト計算も行わない。時計↔一覧、5項目スクロール、ホーム、復帰を実機確認し、描画時間とフレーム間隔を取得する。

### 作業4: 時計・設定保存・設定画面

実装順序・変更箇所・検証条件は[作業4の詳細計画](task4/plan.md)を参照。
2026-09-22完了（ユーザーによる実機確認済み）。実機確認で通知の描画順の不具合を1件修正した。
構成・実機手順は[時計・設定保存・設定画面](task4/settings.md)、
確認状況は[作業4の検証記録](task4/task4-validation.md)を参照。
設定画面を足した分の描画時間は作業7の基準測定へ引き継ぐ。

- [x] TimeServiceにRTC妥当性検証、UTC/JST変換、システム時刻同期、手動保存と失敗時の回復を実装する。
- [x] `launcher` 名前空間にスキーマ版付き設定を保存するSettingsStoreを実装する。全体消去なしで不正値・未知の版・初期化失敗を扱う。
- [x] 日時、輝度、消灯時間、情報表示の設定画面を実装し、保存・キャンセル・ホーム時の編集破棄を接続する。
- [x] 時計の分境界、電池の30秒更新、復帰・設定後の即時更新を実データで動作させる。

成果物は実際の時計として利用できるホームと設定画面。

完了条件: 月末・うるう日・JST日付境界・無効RTC・保存失敗を確認する。再起動後に設定を復元し、キャンセルしたプレビューは元へ戻る。NVS異常でも他名前空間を消去しない。

### 作業5: ストップウォッチ

実装順序・変更箇所・検証条件は[作業5の詳細計画](task5/plan.md)を参照。
2026-09-22完了（ユーザーによる実機確認済み）。UIはM5StopWatch-UserDemoのストップウォッチへ合わせ、
5.1節・5.3節を改訂した。実機確認で描画検証の診断コードの不具合を1件修正した。
構成・実機手順は[ストップウォッチ](task5/stopwatch.md)、
確認状況は[作業5の検証記録](task5/task5-validation.md)を参照。
**1時間以上の長時間計測だけは未実施で、作業7へ送る。**
計測中の表示更新性能は作業7の基準測定へ引き継ぐ。

- [x] StopwatchServiceのReset / Running / Paused、累積時間と単調時刻による計算、ラップを実装する。
- [x] 開始・停止・再開・リセット・ラップを、左右2ボタンのA/Bとタッチで操作できる画面を実装する。
- [x] 可視時だけ表示期限を登録し、画面退場・消灯中は描画を止める。
- [x] 作業6から持ち越した2項目（API失敗時の計測継続、成功時の計測終了）を接続する。

成果物は時計・設定と行き来できるストップウォッチ。

完了条件: 時刻設定の前後変更で計測値が飛ばない。ホームや消灯を挟んでも計測が継続し、再開とリセットが正しい。長時間値・表示上限・表示更新性能を確認する。作業6の残り2項目を消化し、作業6を完了扱いにする。

### 作業6: MultiFirmホスト連携

実装順序・変更箇所・検証条件は[作業6の詳細計画](task6/plan.md)を参照。
2026-09-22に実装・PC検証・ユーザーによる実機確認を完了（作業5より先に実施）。
構成・実機手順は[MultiFirmホスト連携](task6/multifirm.md)、
確認状況は[作業6の検証記録](task6/task6-validation.md)を参照。
2026-09-22に8.2節1を変更し、Readyのスロットは確認画面を挟まず直接起動するようにした（実機確認済み）。
完了条件のうち計測に触れる2項目（API失敗時の計測継続、成功時の計測終了）は作業5で接続し、
同日の実機確認で消化した。**これをもって作業6も完了。**
走査中はフレームレートが体感で下がる。破綻もWDTもないが、作業7では別集計とする。

- [x] MultiFirmAdapterと直列ワーカーを実装し、時計表示後にスロットを走査する。完了通知はUIイベント経由で渡す。
- [x] 各スロットの名前・状態・詳細、非対応レイアウト、起動エラーを表示する。
- [x] 最終決定前の保存、BootCommitting、成功時shutdown、失敗時の画面復帰を接続する。
- [x] 検証中のホームでは画面を戻し、遅延結果が画面遷移や起動を引き起こさないようにする。

成果物はota_1〜ota_3を起動できるホスト。起動確定中のキャンセル制約は8.2節に従う。

完了条件: 正常・空・破損・読取失敗・非対応表を確認する。API失敗時は再起動せず計測が継続し、成功時は計測を終了する。各ゲストへの起動と時計画面への復帰、ホスト更新後のゲスト・設定保持を実機確認する。

### 作業7: 初版の統合試験と基準測定

実装順序・変更箇所・検証条件は[作業7の詳細計画](task7/plan.md)を参照。
2026-09-22に、測定の道具として画面上の統計情報表示を先に実装した（7-0）。
2026-09-23に、製品構成のまま集計を取る `m5stopwatch-measure` と、遷移・スクロール・
計測中・休止を区別する集計を実装した（7-1）。基準測定はこのビルドで行う。
同日に基準測定と9章の受け入れ確認を実施した。**一覧スクロールの30fps超と画面OFF時20mAは未達**で、
合格扱いにしていない。結果は[作業7の検証記録](task7/task7-validation.md)と
[測定記録](measurements.md)の「製品版の測定」を参照。電池駆動時間は作業8へ送った。
作業3の実機ログでフレーム間隔の33.3ms超過と30fps未満の窓がある。
[作業3の検証記録](task3/task3-validation.md)に従い、操作停止時間と継続操作区間を分けて再測定する。
作業5から**1時間以上の長時間計測**を引き継ぐ。9章の「計測」の長時間動作はここで消化する。
ストップウォッチの計測中は25ms周期の継続描画区間なので、スクロール・遷移・走査中とは別に集計する。

- [x] 9章の受け入れ項目を製品版で実施し、結果と未達項目を記録する。作業5から送られた1時間以上の長時間計測を含む。
      未達は一覧スクロールの30fps超と画面OFF時20mAの2件。
- [x] 実fpsと描画処理時間を実機の画面上で読めるようにし、表示の有無で測定値が変わらないことを確かめる。
- [x] 240MHz固定で実fps・フレーム間隔・入力遅延・復帰時間・メモリを測定する。
      一覧スクロールだけが30fpsに届かず、未達として記録した。
- [x] 同条件のUSB電力を測る（画面OFF 35.5mA、アイドル 36.0mA、スクロール 55.0mA）。
      **電池駆動時間は2026-09-23に作業8へ送った。** USB側の電流から電池側を換算しないため、
      電池側の基準測定は施策比較と条件を揃えて作業8で取る。
- [x] 通常ビルドから検証専用バッファ・起動時ベンチを除き、操作・更新・復旧のREADMEを整える。

成果物は初版ファームウェアと製品版の測定記録。既存の試作測定を上書きせず、版・設定・シナリオを識別して追記または別ファイル化する。
測定結果は[測定記録](measurements.md)の「製品版の測定」へ追記し、試作の値は保持した。
実施状況は[作業7の検証記録](task7/task7-validation.md)を参照。

完了条件: 初版機能、データ保護、描画の正しさ、操作中30fps超の評価を完了する。未達は原因と対応を明記し、合格扱いしない。画面OFF時20mAは別途改善目標として残してよいが、現在値を必ず記録する。

### 作業8: 段階的な省電力化

施策の優先順位・根拠・推奨実装順は [作業8の省電力化検討](task8/power-saving-ideas.md) を参照。
進め方・段の分け方・合意事項は[作業8の詳細計画](task8/plan.md)を参照。
2026-09-23に、VBUS中のlight sleep禁止（7.2節）と、電池駆動の測定を3回（基準・DFS＋GPIO化後・light sleep後）に
限ることを決めた。この方針ではUSB側20mAは届かない見込みで、達否は実測で判定する。
同日に電池電圧ロガー（`m5stopwatch-drain`）を実装した（8-0）。
同日に電池駆動の1回目（240MHz固定、4.1→3.7Vで4.48時間）を取り、+16mA調査用のUSBバス活動の履歴（8-2）とDFS（8-3）を実装した。

- [ ] 電池側の基準測定を取る（作業7から送られた項目）。USB側の値とは別表にする。
- [x] USB Serial JTAGのCDCポートを開くと全条件に +16mA 以上が乗り、閉じても
      リセットまで戻らない件を調べる。7.1節の `usbSessionActive` の想定より強い挙動で、
      画面OFF 20mA目標の評価にも影響する。
      2026-09-23に調べたが再現せず、原因は未特定。リセット後に読む運用で扱う（[8-2](task8/plan.md)）。
- [x] 入力をGPIO通知と期限待機へ移し、不要なポーリングを減らす。
      2026-09-23。無操作時の起床は毎秒100回→1回。USB側の電流は誤差内（[8-4](task8/plan.md)）。
- [x] 操作時の性能確保と待機時のDFSを導入し、240MHz固定と比較する。
      2026-09-23。描画性能は不変、画面OFF 35.5→30.5mA・時計静止 36.0→31.5mA（[8-3](task8/plan.md)）。
- [ ] tickless idle・自動light sleep、USB通信維持用ロックを導入する。
- [ ] 各段階で入力、計測連続性、時刻、復帰、USB、描画性能と電力を再評価する。

成果物は効果を確認した省電力設定と比較記録。施策を一括投入せず、問題が起きたら直前の構成へ戻せるようにする。deep sleepやMultiFirm共有時刻APIはこの作業に含めない。

完了条件: 性能・操作の受け入れ条件を維持しながら電力と電池持ちの改善を実測する。USB側20mAの達否と電池駆動の結果を分けて示し、未達の場合は残る消費と次の調査事項を記録する。

### 作業9: UI設計の整理とリスト共通化

責務分割・目標構成・実装順序・検証条件は[作業9の詳細計画](task9/plan.md)を参照。
2026-09-23に、共通描画基盤と機能固有UIの境界を整理し、アプリ一覧と設定トップメニューの
配置・操作・描画・キャッシュを共通リストへまとめる方針を合意した。
設定トップの見た目とスクロール挙動の変更は許容し、日時・輝度等の編集画面は既存仕様を維持する。
本項は未実装の改修計画であり、4章の構成・契約は実装完了時に更新する。

- [x] 現行の操作・描画・性能・メモリとテストの基準を確認する。
      2026-09-23に9-0完了。[改修前の基準確認](task9/baseline.md)にビルド・テスト・実機測定を記録。
      一覧の定常間隔33.3ms以内は未達として保持し、測定後は通常版へ復帰済み。
- [ ] 共通型、機能別表示モデル、描画から独立した実効設定を分離する。
- [ ] 共通リストを抽出して両画面へ適用し、一覧・トーストの具体的な描画をRendererから移す。
- [ ] 機能別にファイルを配置し、画面遷移、ホーム・一覧操作、文字盤管理、サービス所有を整理する。
- [ ] 全レイヤー共通のFramePlanと背景復元の契約を維持し、差分描画・確保失敗・操作・電源の回帰を検証する。
- [ ] 両リストの性能とメモリを比較し、検証記録と設計書を更新する。

成果物は共通リストと責務分割を適用した実装、検証記録、更新した設計書。
完了条件は詳細計画の8節による。作業7の一覧スクロール性能未達を引き継ぎ、
構造改善の完了と30fps超・定常間隔33.3ms以内の達成は別々に評価する。作業8の省電力設定も比較条件に記録する。

## 参照

- [測定記録](measurements.md)
- [試作の実行手順](../measurement/README.md)
- [WatchFace契約](../measurement/src/ui/WatchFace.h)
- [FramePlan](../measurement/src/ui/Element.h)
- [MultiFirm設計](../../M5StopWatch-MultiFirm/docs/multifirm-plan.md)
- [MultiFirmホストAPI](../../M5StopWatch-MultiFirm/src/multifirm_host.h)
- [MuteHid省電力記録](../../M5StopWatch-MuteHid/docs/power-saving-ideas.md)
